ParserResult Parser_parseExpression(
    Parser *this,
    ParserInStream *stream,
    ParserFrame *frame,
    Type desiredType,
    Expression *out_expression
) {
    ParserResult result = ParserResult_Success;

    *out_expression = Expression_NULL

    return result;
}